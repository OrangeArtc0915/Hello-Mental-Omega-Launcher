# HMOL 启动器依赖锁定
# 固定版本号以保证可复现构建

# GUI 框架
PySide6>=6.6.0,<6.9.0

# 加密（核心安全模块）
cryptography>=41.0.0,<50.0.0

# Microsoft 认证
msal>=1.24.0,<2.0.0
requests>=2.31.0,<3.0.0
urllib3>=2.0.0,<3.0.0

# 压缩包处理
py7zr>=0.21.0,<1.0.0
rarfile>=4.1.0,<5.0.0

# 图像处理（图标等）
Pillow>=10.0.0,<12.0.0

# PyInstaller（仅构建时需要，发布时不需要）
# pyinstaller>=6.0.0
# pycryptodome>=3.18.0  # 用于 --key 字节码加密
